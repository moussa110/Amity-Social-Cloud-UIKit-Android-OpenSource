<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <data>

        <import type="com.amity.socialcloud.uikit.common.common.views.AmityColorShade" />

        <variable
            name="viewModel"
            type="com.amity.socialcloud.uikit.chat.editMessage.AmityEditMessageViewModel" />
    </data>

    <androidx.constraintlayout.widget.ConstraintLayout
        android:id="@+id/lMessage"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:fitsSystemWindows="true"
        android:focusable="false"
        tools:context="com.amity.socialcloud.uikit.chat.editMessage.AmityEditMessageActivity">

        <ImageView
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:scaleType="fitXY"
            android:src="@drawable/auth_bg"
            tools:ignore="ContentDescription" />

        <View
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:background="@color/fb_darker_gray_bg_transparent_15"/>

        <include
            android:id="@+id/emToolBar"
            layout="@layout/amity_edit_msg_bar"
            app:viewModel="@{viewModel}" />

        <View
            android:id="@+id/divider"
            style="@style/AmityDivider"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toBottomOf="@id/emToolBar" />

        <com.google.android.material.textfield.TextInputEditText
            android:id="@+id/etMessage"
            style="@style/AmityTextStyleBody"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_margin="@dimen/amity_padding_m1"
            android:background="@null"
            android:textColor="@color/yellowColor"
            android:text="@={viewModel.message}"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toBottomOf="@id/divider" />


    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>